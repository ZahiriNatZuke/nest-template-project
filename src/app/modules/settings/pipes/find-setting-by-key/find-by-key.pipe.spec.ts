import { FindSettingByKeyPipe } from './find-setting-by-key.pipe';

describe('FindSettingByKeyPipe', () => {
  it('should be defined', () => {
    expect(FindSettingByKeyPipe).toBeDefined();
  });
});
